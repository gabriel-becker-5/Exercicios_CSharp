// Crie um programa que exiba todos os numeros pares até 20

int repeticaoMaximaLoop = 10;
int numeroInicial = 0;

for (int i = 0; i < repeticaoMaximaLoop; i++)
{
    numeroInicial += 2;
    Console.WriteLine("Número par: " + numeroInicial);
}