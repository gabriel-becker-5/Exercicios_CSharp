// crie um programa que: declare um array com 5 numeros
// exiba todos os valores no console

int[] numeros = [1, 2, 3, 4, 5];

for (int i = 0; i < numeros.Length; i++)
{
    Console.WriteLine(numeros[i]);
}